Built a full-stack real-time video calling web application using WebRTC, Socket.IO, and React.js. Integrated peer-to-peer communication with audio/video streaming, online presence detection
Secure peer-to-peer video and audio calls using WebRTC + Simple-Peer.
Socket.IO signaling for call setup, accept/reject, and online user tracking.
Interactive UI with mic/camera toggles, call notifications, and live status indicators.
JWT-based authentication and RESTful APIs for user management (Node.js + Express).
Real-time data synchronization between users via WebSockets.
